info:
  name: Mobile Trading Alert (MTA) Details
  type: http
  seq: 6

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/account/mta
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
