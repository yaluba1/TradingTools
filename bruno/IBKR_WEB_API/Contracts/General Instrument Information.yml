info:
  name: General Instrument Information
  type: http
  seq: 4

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/contract/{{conid}}/info
  auth: inherit

runtime:
  variables:
    - name: conid
      value: "468091282"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
