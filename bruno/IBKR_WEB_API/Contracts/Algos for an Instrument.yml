info:
  name: Algos for an Instrument
  type: http
  seq: 3

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/contract/{{conid}}/algos
  auth: inherit

runtime:
  variables:
    - name: conid
      value: "468091282"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
