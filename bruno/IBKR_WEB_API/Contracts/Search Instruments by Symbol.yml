info:
  name: Search Instruments by Symbol
  type: http
  seq: 10

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/secdef/search?symbol=ZIM
  params:
    - name: symbol
      value: ZIM
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
