info:
  name: Event Contract Schedules
  type: http
  seq: 5

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/forecast/contract/schedules?conid=849540484
  params:
    - name: conid
      value: "849540484"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
