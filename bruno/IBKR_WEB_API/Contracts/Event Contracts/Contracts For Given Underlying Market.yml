info:
  name: Contracts For Given Underlying Market
  type: http
  seq: 3

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/forecast/contract/market?underlyingConid=848765505
  params:
    - name: underlyingConid
      value: "848765505"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
