info:
  name: Event Contract Categories
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/forecast/category/tree
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
