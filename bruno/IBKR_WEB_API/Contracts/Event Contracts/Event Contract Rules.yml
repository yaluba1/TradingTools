info:
  name: Event Contract Rules
  type: http
  seq: 4

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/forecast/contract/rules?conid=848765505
  params:
    - name: conid
      value: "848765505"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
