info:
  name: Trading Schedule by Symbol
  type: http
  seq: 16

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/contract/trading-schedule?conid=468091282
  params:
    - name: conid
      value: "468091282"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
