info:
  name: Search Stocks by Symbol
  type: http
  seq: 17

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/trsrv/stocks?symbols=ZIM,EIX,DNLI
  params:
    - name: symbols
      value: ZIM,EIX,DNLI
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
