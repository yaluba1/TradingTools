info:
  name: Instrument Attributes Detail
  type: http
  seq: 9

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/secdef/info?conid=468091282
  params:
    - name: conid
      value: "468091282"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
