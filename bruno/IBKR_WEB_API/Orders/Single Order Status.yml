info:
  name: Single Order Status
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/account/order/status/{{orderId}}
  auth: inherit

runtime:
  variables:
    - name: orderId
      value: "761318694"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
