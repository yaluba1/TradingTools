info:
  name: Validate SSO Web API Session
  type: http
  seq: 4

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/sso/validate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
