info:
  name: Terminate Web API Session
  type: http
  seq: 3

http:
  method: POST
  url: https://127.0.0.1:{{gw_port}}/v1/api/logout
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
