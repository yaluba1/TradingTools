info:
  name: Brokerage Session Status
  type: http
  seq: 2

http:
  method: POST
  url: https://127.0.0.1:{{gw_port}}/v1/api/iserver/auth/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
