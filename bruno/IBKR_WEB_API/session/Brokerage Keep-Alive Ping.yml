info:
  name: Brokerage Keep-Alive Ping
  type: http
  seq: 5

http:
  method: POST
  url: https://127.0.0.1:{{gw_port}}/v1/api/tickle
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
