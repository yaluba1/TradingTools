info:
  name: Account Values
  type: http
  seq: 4

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/iserver/account/{{accountId}}/summary
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
