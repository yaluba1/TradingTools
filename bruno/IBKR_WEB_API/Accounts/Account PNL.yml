info:
  name: Account PNL
  type: http
  seq: 2

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/iserver/account/pnl/partitioned
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
