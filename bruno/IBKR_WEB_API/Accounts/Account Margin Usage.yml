info:
  name: Account Margin Usage
  type: http
  seq: 7

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/iserver/account/{{accountId}}/summary/margins
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
