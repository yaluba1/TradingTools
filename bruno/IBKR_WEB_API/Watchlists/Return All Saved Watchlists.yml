info:
  name: Return All Saved Watchlists
  type: http
  seq: 4

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/watchlists
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
