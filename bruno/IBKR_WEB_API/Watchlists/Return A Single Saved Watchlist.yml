info:
  name: Return A Single Saved Watchlist
  type: http
  seq: 2

http:
  method: GET
  url: https://localhost:{{gw_port}}/v1/api/iserver/watchlist?id=109
  params:
    - name: id
      value: "109"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
