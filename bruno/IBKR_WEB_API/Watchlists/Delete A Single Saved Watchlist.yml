info:
  name: Delete A Single Saved Watchlist
  type: http
  seq: 3

http:
  method: DELETE
  url: https://localhost:{{gw_port}}/v1/api/iserver/watchlist?id=1000
  params:
    - name: id
      value: "1000"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
