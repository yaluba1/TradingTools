info:
  name: All Account Positions in an Instrument
  type: http
  seq: 2

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/portfolio/positions/{{conid}}
  auth: inherit

runtime:
  variables:
    - name: conid
      value: "547605251"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
