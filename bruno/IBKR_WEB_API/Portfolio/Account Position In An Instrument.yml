info:
  name: Account Position In An Instrument
  type: http
  seq: 10

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/portfolio/{{accountId}}/position/{{conId}}
  auth: inherit

runtime:
  variables:
    - name: conId
      value: "547605251"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
