info:
  name: Account Attributes
  type: http
  seq: 6

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/portfolio/{{accountId}}/meta
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
