info:
  name: Account Positions
  type: http
  seq: 8

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/portfolio/{{accountId}}/positions/{{pageId}}
  auth: inherit

runtime:
  variables:
    - name: pageId
      value: "0"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
