info:
  name: Account Ledger
  type: http
  seq: 5

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/portfolio/{{accountId}}/ledger
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
