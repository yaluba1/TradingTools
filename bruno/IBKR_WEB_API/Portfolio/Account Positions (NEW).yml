info:
  name: Account Positions (NEW)
  type: http
  seq: 11

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/portfolio2/{{accountId}}/positions/
  auth: inherit

runtime:
  variables:
    - name: pageId
      value: "0"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
