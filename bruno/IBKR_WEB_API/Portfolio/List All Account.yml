info:
  name: List All Account
  type: http
  seq: 1

http:
  method: GET
  url: https://127.0.0.1:{{gw_port}}/v1/api/portfolio/accounts
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
